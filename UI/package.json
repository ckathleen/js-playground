{
  "name": "js-playground",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "watch": "watchify script.js -o bundled.js -v -t [ babelify --presets [ es2015 ] ]",
    "build": "browserify script.js -o bundle.js -t [ babelify --presets [ es2015 ] ] -v"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.0",
    "watchify": "^3.7.0"
  },
  "old-way-browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ]
    ]
  }
}
